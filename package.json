{
  "name": "bounter",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd packages/student-ui && npm install && npm run build",
    "dev": "cd packages/student-ui && npm run dev"
  },
  "devDependencies": {
    "@vercel/node": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
